{
  "name": "@skill-share/frontend",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite --host 127.0.0.1 --port 4180 --strictPort",
    "build": "vite build",
    "preview": "vite preview --host 0.0.0.0 --port 4180",
    "check": "node --check src/main.js && node --check src/api.js && node --check src/realtime.js"
  },
  "dependencies": {
    "lucide": "^0.468.0"
  },
  "devDependencies": {
    "vite": "^7.0.0"
  }
}
